# ระบบตรวจสอบการเช่ารถซ้ำกัน (Duplicate Rental Check System)

## ภาพรวม

ระบบนี้ถูกออกแบบมาเพื่อป้องกันการเช่ารถซ้ำกันในวันเดียวกัน โดยจะตรวจสอบช่วงเวลาการเช่าและแจ้งเตือนผู้ใช้เมื่อมีการทับซ้อนของช่วงเวลา

## ฟีเจอร์หลัก

### 1. การตรวจสอบการซ้ำกันแบบ Real-time

-   ตรวจสอบการทับซ้อนของช่วงเวลาการเช่า
-   แจ้งเตือนทันทีเมื่อเลือกรถที่ถูกเช่าในช่วงเวลาดังกล่าว
-   รองรับการตรวจสอบทั้ง Car ID และ License Plate

### 2. การแสดงรถที่พร้อมใช้งาน

-   แสดงรายการรถที่พร้อมใช้งานในช่วงเวลาที่เลือก
-   อัปเดต dropdown รถยนต์ตามผลการตรวจสอบ
-   แสดงสถานะรถแต่ละคัน

### 3. Dashboard แบบ Real-time

-   แสดงสถิติรถยนต์ทั้งหมด
-   แสดงการเช่ารถปัจจุบัน
-   แสดงรถที่พร้อมใช้งาน

## การทำงานของระบบ

### การตรวจสอบการซ้ำกัน

ระบบจะตรวจสอบการทับซ้อนของช่วงเวลาใน 3 กรณี:

1. **วันที่เริ่มใหม่อยู่ในช่วงวันที่เช่าเดิม**

    - `start_date_new <= start_date_old < end_date_new`

2. **วันที่สิ้นสุดใหม่อยู่ในช่วงวันที่เช่าเดิม**

    - `start_date_new < end_date_old <= end_date_new`

3. **ช่วงเวลาใหม่ครอบคลุมช่วงเวลาเดิม**
    - `start_date_new >= start_date_old AND end_date_new <= end_date_old`

### การกรองข้อมูล

-   ไม่รวมการเช่าที่มีสถานะ "cancel"
-   ไม่รวมการเช่าปัจจุบันที่กำลังแก้ไข (สำหรับการอัปเดต)

## การใช้งาน

### 1. หน้าเพิ่มการเช่ารถใหม่

1. เลือกวันที่และเวลาเริ่ม/สิ้นสุด
2. คลิกปุ่ม "ตรวจสอบ" เพื่อดูรถที่พร้อมใช้งาน
3. เลือกรถยนต์จาก dropdown
4. ระบบจะตรวจสอบการซ้ำกันอัตโนมัติ

### 2. หน้าแก้ไขการเช่ารถ

1. ระบบจะตรวจสอบการซ้ำกันเมื่อมีการเปลี่ยนแปลงวันที่
2. แสดงข้อความแจ้งเตือนหากมีการซ้ำกัน

### 3. Dashboard

1. แสดงสถิติรถยนต์แบบ Real-time
2. แสดงการเช่ารถปัจจุบัน
3. แสดงรถที่พร้อมใช้งาน

## โครงสร้างไฟล์

### Models

-   `Rental.php` - เพิ่ม methods สำหรับตรวจสอบการซ้ำกัน
-   `Car.php` - โมเดลรถยนต์

### Controllers

-   `RentalController.php` - จัดการการเช่ารถและการตรวจสอบ

### Views

-   `rentals/create.blade.php` - หน้าเพิ่มการเช่ารถใหม่
-   `pages/dashboard.blade.php` - หน้า Dashboard

### Routes

-   `web.php` - กำหนด routes สำหรับระบบ

## API Endpoints

### 1. ตรวจสอบรถที่พร้อมใช้งาน

```
POST /rentals/check-available-cars
Body: { start_date, end_date }
```

### 2. ตรวจสอบการซ้ำกัน

```
POST /rentals/check-duplicate
Body: { car_id, start_date, end_date, rental_id? }
```

### 3. ข้อมูลสถิติ

```
GET /api/statistics
```

### 4. การเช่ารถปัจจุบัน

```
GET /api/current-rentals
```

### 5. รถที่พร้อมใช้งาน

```
GET /api/available-cars
```

## การติดตั้งและใช้งาน

### 1. ติดตั้ง Dependencies

```bash
composer install
npm install
```

### 2. ตั้งค่าฐานข้อมูล

```bash
php artisan migrate
php artisan db:seed
```

### 3. รันระบบ

```bash
php artisan serve
npm run dev
```

## การปรับแต่ง

### 1. เพิ่มการตรวจสอบเพิ่มเติม

สามารถเพิ่มเงื่อนไขการตรวจสอบใน `Rental::checkDuplicateRental()` method

### 2. ปรับแต่งข้อความแจ้งเตือน

แก้ไขข้อความใน `RentalController` และ view files

### 3. เพิ่มฟีเจอร์ใหม่

สามารถเพิ่มการตรวจสอบอื่นๆ เช่น การตรวจสอบระยะทาง, ราคา, ฯลฯ

## ข้อควรระวัง

1. **Performance**: ระบบตรวจสอบการซ้ำกันอาจช้าถ้ามีข้อมูลจำนวนมาก
2. **Timezone**: ต้องระวังเรื่อง timezone ของเซิร์ฟเวอร์และฐานข้อมูล
3. **Validation**: ควรเพิ่ม validation เพิ่มเติมตามความต้องการ

## การพัฒนาต่อ

1. เพิ่มระบบแจ้งเตือนแบบ Push Notification
2. เพิ่มการรายงานสถิติแบบ Real-time
3. เพิ่มระบบจัดการคิวการเช่ารถ
4. เพิ่มการตรวจสอบความพร้อมของรถ (เชื้อเพลิง, สภาพ, ฯลฯ)

## การสนับสนุน

หากมีปัญหาหรือต้องการความช่วยเหลือ กรุณาติดต่อทีมพัฒนา
