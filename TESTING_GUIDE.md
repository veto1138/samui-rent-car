# คู่มือการทดสอบระบบตรวจสอบการเช่ารถซ้ำกัน

## การทดสอบเบื้องต้น

### 1. ทดสอบการตรวจสอบการซ้ำกัน

#### ขั้นตอนที่ 1: สร้างการเช่ารถครั้งแรก

1. ไปที่หน้า `/rentals/create`
2. กรอกข้อมูลการเช่ารถ:
    - ชื่อ: สมชาย ใจดี
    - นามสกุล: ใจดี
    - เลขบัตรประชาชน: 1234567890123
    - เบอร์โทร: 0812345678
    - ที่อยู่: 123 ถนนสุขุมวิท กรุงเทพฯ
    - ชื่อพยาน: สมหญิง ใจดี
    - นามสกุลพยาน: ใจดี
    - วันที่เริ่ม: วันนี้ เวลา 10:00
    - วันที่สิ้นสุด: พรุ่งนี้ เวลา 10:00
    - สถานที่รับรถ: สนามบินดอนเมือง
    - สถานที่ส่งรถ: สนามบินดอนเมือง
    - เลือกรถยนต์: รถคันที่ 1
    - อัปโหลดรูปภาพที่จำเป็น
3. คลิก "บันทึกข้อมูลการเช่ารถ"

#### ขั้นตอนที่ 2: ทดสอบการเช่ารถซ้ำกัน

1. ไปที่หน้า `/rentals/create` อีกครั้ง
2. กรอกข้อมูลการเช่ารถใหม่:
    - ชื่อ: สมปอง รักดี
    - นามสกุล: รักดี
    - เลขบัตรประชาชน: 9876543210987
    - เบอร์โทร: 0987654321
    - ที่อยู่: 456 ถนนรัชดาภิเษก กรุงเทพฯ
    - ชื่อพยาน: สมศรี รักดี
    - นามสกุลพยาน: รักดี
    - วันที่เริ่ม: วันนี้ เวลา 12:00 (ทับซ้อนกับครั้งแรก)
    - วันที่สิ้นสุด: พรุ่งนี้ เวลา 12:00
    - สถานที่รับรถ: สนามบินดอนเมือง
    - สถานที่ส่งรถ: สนามบินดอนเมือง
    - เลือกรถยนต์: รถคันเดียวกัน (คันที่ 1)
3. ระบบควรแสดงข้อความแจ้งเตือน: "รถยนต์คันนี้ถูกเช่าในช่วงเวลาดังกล่าวแล้ว"

### 2. ทดสอบการตรวจสอบรถที่พร้อมใช้งาน

#### ขั้นตอนที่ 1: เลือกช่วงเวลา

1. ไปที่หน้า `/rentals/create`
2. เลือกวันที่และเวลาเริ่ม/สิ้นสุด
3. คลิกปุ่ม "ตรวจสอบ"

#### ขั้นตอนที่ 2: ตรวจสอบผลลัพธ์

1. ระบบควรแสดงรายการรถที่พร้อมใช้งาน
2. รถที่ถูกเช่าในช่วงเวลาดังกล่าวจะไม่แสดงในรายการ
3. Dropdown รถยนต์จะอัปเดตตามผลการตรวจสอบ

### 3. ทดสอบการแก้ไขการเช่ารถ

#### ขั้นตอนที่ 1: แก้ไขการเช่ารถ

1. ไปที่หน้า `/rentals/{id}/edit`
2. เปลี่ยนวันที่เริ่มหรือสิ้นสุดให้ทับซ้อนกับการเช่าอื่น
3. ระบบควรแสดงข้อความแจ้งเตือนการซ้ำกัน

#### ขั้นตอนที่ 2: ตรวจสอบการบันทึก

1. ระบบควรไม่อนุญาตให้บันทึกข้อมูลที่มีการซ้ำกัน
2. แสดงข้อความ error ที่เหมาะสม

## การทดสอบ Dashboard

### 1. ทดสอบการแสดงสถิติ

1. ไปที่หน้า `/dashboard`
2. ตรวจสอบว่าสถิติแสดงถูกต้อง:
    - รถทั้งหมด
    - รถที่พร้อมใช้งาน
    - กำลังเช่า
    - อยู่ในการซ่อม

### 2. ทดสอบการแสดงการเช่ารถปัจจุบัน

1. ตรวจสอบว่าการเช่ารถปัจจุบันแสดงถูกต้อง
2. แสดงสถานะที่ถูกต้อง (pending, using, success, cancel)

### 3. ทดสอบการแสดงรถที่พร้อมใช้งาน

1. ตรวจสอบว่ารถที่พร้อมใช้งานแสดงถูกต้อง
2. ไม่แสดงรถที่ถูกเช่าหรืออยู่ในการซ่อม

## การทดสอบ API

### 1. ทดสอบ API ตรวจสอบรถที่พร้อมใช้งาน

```bash
curl -X POST http://localhost:8000/rentals/check-available-cars \
  -H "Content-Type: application/json" \
  -H "X-CSRF-TOKEN: {csrf_token}" \
  -d '{
    "start_date": "2025-01-20 10:00:00",
    "end_date": "2025-01-21 10:00:00"
  }'
```

### 2. ทดสอบ API ตรวจสอบการซ้ำกัน

```bash
curl -X POST http://localhost:8000/rentals/check-duplicate \
  -H "Content-Type: application/json" \
  -H "X-CSRF-TOKEN: {csrf_token}" \
  -d '{
    "car_id": 1,
    "start_date": "2025-01-20 10:00:00",
    "end_date": "2025-01-21 10:00:00"
  }'
```

### 3. ทดสอบ API ข้อมูลสถิติ

```bash
curl http://localhost:8000/api/statistics
```

## สถานการณ์การทดสอบ

### สถานการณ์ที่ 1: การเช่าทับซ้อนบางส่วน

-   การเช่าแรก: 10:00 - 18:00
-   การเช่าที่สอง: 14:00 - 22:00
-   ผลลัพธ์: ควรแสดงข้อความแจ้งเตือนการซ้ำกัน

### สถานการณ์ที่ 2: การเช่าครอบคลุม

-   การเช่าแรก: 10:00 - 18:00
-   การเช่าที่สอง: 08:00 - 20:00
-   ผลลัพธ์: ควรแสดงข้อความแจ้งเตือนการซ้ำกัน

### สถานการณ์ที่ 3: การเช่าไม่ทับซ้อน

-   การเช่าแรก: 10:00 - 18:00
-   การเช่าที่สอง: 19:00 - 22:00
-   ผลลัพธ์: ควรอนุญาตให้เช่าได้

### สถานการณ์ที่ 4: การเช่าในวันต่างกัน

-   การเช่าแรก: วันนี้ 10:00 - 18:00
-   การเช่าที่สอง: พรุ่งนี้ 10:00 - 18:00
-   ผลลัพธ์: ควรอนุญาตให้เช่าได้

## การตรวจสอบ Logs

### 1. ตรวจสอบ Laravel Logs

```bash
tail -f storage/logs/laravel.log
```

### 2. ตรวจสอบ Database

```sql
-- ตรวจสอบการเช่ารถปัจจุบัน
SELECT * FROM rentals
WHERE status IN ('pending', 'using')
AND start_date <= NOW()
AND end_date >= NOW();

-- ตรวจสอบการทับซ้อนของช่วงเวลา
SELECT r1.*, r2.*
FROM rentals r1
JOIN rentals r2 ON r1.id != r2.id
WHERE r1.car_id = r2.car_id
AND r1.status != 'cancel'
AND r2.status != 'cancel'
AND (
    (r1.start_date <= r2.start_date AND r1.end_date > r2.start_date) OR
    (r1.start_date < r2.end_date AND r1.end_date >= r2.end_date) OR
    (r1.start_date >= r2.start_date AND r1.end_date <= r2.end_date)
);
```

## การแก้ไขปัญหาที่พบบ่อย

### ปัญหา 1: ระบบไม่แสดงข้อความแจ้งเตือนการซ้ำกัน

**สาเหตุที่เป็นไปได้:**

-   JavaScript ไม่ทำงาน
-   CSRF token ไม่ถูกต้อง
-   Route ไม่ถูกต้อง

**วิธีแก้ไข:**

1. ตรวจสอบ Console ใน Developer Tools
2. ตรวจสอบ Network tab ใน Developer Tools
3. ตรวจสอบ Laravel logs

### ปัญหา 2: การตรวจสอบช้า

**สาเหตุที่เป็นไปได้:**

-   ข้อมูลในฐานข้อมูลจำนวนมาก
-   ไม่มี index ที่เหมาะสม

**วิธีแก้ไข:**

1. เพิ่ม index สำหรับ start_date และ end_date
2. เพิ่ม index สำหรับ car_id และ status

### ปัญหา 3: Timezone ไม่ถูกต้อง

**สาเหตุที่เป็นไปได้:**

-   การตั้งค่า timezone ใน PHP
-   การตั้งค่า timezone ในฐานข้อมูล

**วิธีแก้ไข:**

1. ตรวจสอบการตั้งค่า timezone ใน config/app.php
2. ตรวจสอบการตั้งค่า timezone ในฐานข้อมูล

## การทดสอบ Performance

### 1. ทดสอบด้วยข้อมูลจำนวนมาก

1. สร้างข้อมูลการเช่ารถจำนวน 1000 รายการ
2. ทดสอบความเร็วในการตรวจสอบการซ้ำกัน
3. ตรวจสอบการใช้ memory

### 2. ทดสอบการทำงานพร้อมกัน

1. เปิดหลายแท็บพร้อมกัน
2. ทดสอบการตรวจสอบการซ้ำกันพร้อมกัน
3. ตรวจสอบความถูกต้องของผลลัพธ์

## สรุป

การทดสอบระบบตรวจสอบการเช่ารถซ้ำกันควรครอบคลุม:

1. การตรวจสอบการซ้ำกันแบบ Real-time
2. การแสดงรถที่พร้อมใช้งาน
3. การทำงานของ Dashboard
4. การทำงานของ API
5. Performance และความเสถียร
6. การจัดการข้อผิดพลาด

หากพบปัญหาหรือต้องการความช่วยเหลือเพิ่มเติม กรุณาติดต่อทีมพัฒนา
